object TopAxioms { 
  val problem =
    List(
      // Name: lemma_1a_1
      // Role: negated_conjecture
      List("basis(cx, f)")),
      // Name: lemma_1a_2
      // Role: negated_conjecture
      List("~subset_sets(union_of_members(top_of_basis(f)), cx)")))
}